# 关于设计动效的几点思考

最近认真读了 [emilkowalski/skills](https://github.com/emilkowalski/skills) 里的设计准则，有几点特别触动我：

## 1. 动效要克制

好的动效不是炫技，而是为了**引导注意力**和**传递状态变化**。超过 300ms 的动效会让用户感到拖沓。

## 2. 物理要正确

比如下拉菜单应该从触发按钮的位置展开（`transform-origin`），而不是从屏幕中心弹出来。

## 3. 永远尊重用户偏好

必须监听 `prefers-reduced-motion`，为对动效敏感的用户提供关闭选项。

这套博客的交互就完全遵循了这些原则。
